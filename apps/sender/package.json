{
  "name": "@pitwall/sender",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json && electron dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@pitwall/f1-parser": "workspace:*",
    "@pitwall/shared-types": "workspace:*",
    "electron": "^35.2.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
